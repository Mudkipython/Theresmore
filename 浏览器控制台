// 每次刷新页面后先运行一次
MainStore = Object.values(document.querySelector("#main-tabs"))[1]
  .children._owner.stateNode.props.MainStore

// 数量可以自己改
const AMOUNT = 999999999

// 普通基础资源
MainStore.ResourcesStore.addResource("gold", AMOUNT)              // 黄金
MainStore.ResourcesStore.addResource("food", AMOUNT)              // 食物
MainStore.ResourcesStore.addResource("wood", AMOUNT)              // 木材
MainStore.ResourcesStore.addResource("stone", AMOUNT)             // 石头
MainStore.ResourcesStore.addResource("research", AMOUNT)          // 研究
MainStore.ResourcesStore.addResource("faith", AMOUNT)             // 信念
MainStore.ResourcesStore.addResource("mana", AMOUNT)              // 法力

// 远古 / 青铜 / 铁器资源
MainStore.ResourcesStore.addResource("tools", AMOUNT)             // 工具
MainStore.ResourcesStore.addResource("copper", AMOUNT)            // 铜
MainStore.ResourcesStore.addResource("iron", AMOUNT)              // 铁

// 动物资源
MainStore.ResourcesStore.addResource("cow", AMOUNT)               // 牛
MainStore.ResourcesStore.addResource("horse", AMOUNT)             // 马

// 中后期资源
MainStore.ResourcesStore.addResource("building_material", AMOUNT) // 原料
MainStore.ResourcesStore.addResource("steel", AMOUNT)             // 钢
MainStore.ResourcesStore.addResource("crystal", AMOUNT)           // 水晶
MainStore.ResourcesStore.addResource("supplies", AMOUNT)          // 补给
MainStore.ResourcesStore.addResource("saltpetre", AMOUNT)         // 硝石
MainStore.ResourcesStore.addResource("natronite", AMOUNT)         // 钠红石

// 声誉 / 幸运类
MainStore.ResourcesStore.addResource("fame", AMOUNT)              // 声誉
MainStore.ResourcesStore.addResource("luck", AMOUNT)              // 幸运石

// 转生 / 传承特殊资源
MainStore.ResourcesStore.addResource("legacy", AMOUNT)            // 传承点
MainStore.ResourcesStore.addResource("relic", AMOUNT)             // 遗物
MainStore.ResourcesStore.addResource("coin", AMOUNT)              // 代币
MainStore.ResourcesStore.addResource("tome_wisdom", AMOUNT)       // 智慧卷轴
MainStore.ResourcesStore.addResource("gem", AMOUNT)               // 宝石
MainStore.ResourcesStore.addResource("titan_gift", AMOUNT)        // 泰坦的礼物
MainStore.ResourcesStore.addResource("light", AMOUNT)             // 光明
